wnat to add the following: 

x - block pushing to main directly. 
x - have ot open pull request. 
x - pull requests needs to have the tests passing.
x - add an example for security scanning (unsafe code)
x - do not allow merge unsafe code (security scanning)